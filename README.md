# Typescript Design Patterns

In this repo, I'm implementing examples of common design patterns.

## How to run

To play a specific example, first do `npm install`. Then run `npm start [src/pattern/index.ts]`.
